-- Spider Hub | By: SpiDev
-- Auto Level Script (Max Level 2800)

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Spider Hub | Blox Fruits",
   LoadingTitle = "Spider Hub Loading...",
   LoadingSubtitle = "By: SpiDev",
   ConfigurationSaving = { Enabled = false }
})

local Tab = Window:CreateTab("Main Farm", 4483362458)

_G.AutoFarm = false
_G.SelectedWeapon = "Melee"

local Toggle = Tab:CreateToggle({
   Name = "Auto Level Farm",
   CurrentValue = false,
   Flag = "AutoLevelToggle",
   Callback = function(Value)
      _G.AutoFarm = Value
   end,
})

local Dropdown = Tab:CreateDropdown({
   Name = "Select Weapon",
   Options = {"Melee", "Sword", "Blox Fruit"},
   CurrentOption = {"Melee"},
   MultipleOptions = false,
   Flag = "WeaponDropdown",
   Callback = function(Option)
      _G.SelectedWeapon = Option[1]
   end,
})

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualUser = game:GetService("VirtualUser")

-- Auto Equip Weapon
local function equipWeapon()
    local backpack = LocalPlayer.Backpack
    local character = LocalPlayer.Character
    if not character then return end
    
    for _, tool in pairs(backpack:GetChildren()) do
        if tool:IsA("Tool") then
            if tool.ToolTip == _G.SelectedWeapon or tool.Name:find(_G.SelectedWeapon) or (_G.SelectedWeapon == "Melee" and tool.ToolTip == "Melee") then
                character.Humanoid:EquipTool(tool)
                break
            end
        end
    end
end

-- Noclip to prevent anti-cheat rubberband
task.spawn(function()
    while task.wait() do
        if _G.AutoFarm and LocalPlayer.Character then
            for _, part in pairs(LocalPlayer.Character:GetChildren()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
    end
end)

-- Tween movement function
local currentTween = nil
local function bypassTween(targetCFrame)
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return end
    local hrp = character.HumanoidRootPart
    
    local distance = (hrp.Position - targetCFrame.Position).Magnitude
    if distance < 10 then 
        hrp.CFrame = targetCFrame
        return 
    end
    
    local speed = 320
    local tweenInfo = TweenInfo.new(distance / speed, Enum.EasingStyle.Linear)
    
    if currentTween then currentTween:Cancel() end
    currentTween = TweenService:Create(hrp, tweenInfo, {CFrame = targetCFrame})
    currentTween:Play()
end

-- Full Quests Database (1 to 2800 Level)
local function getQuestData()
    local level = LocalPlayer.Data.Level.Value
    
    -- First Sea (1 - 700)
    if level >= 1 and level < 15 then
        return {QuestName = "BanditQuest1", QuestNum = 1, MobName = "Bandit", NpcCFrame = CFrame.new(1059, 16, 1548)}
    elseif level >= 15 and level < 30 then
        return {QuestName = "JungleQuest", QuestNum = 1, MobName = "Monkey", NpcCFrame = CFrame.new(-1598, 36, 153)}
    elseif level >= 30 and level < 60 then
        return {QuestName = "JungleQuest", QuestNum = 2, MobName = "Gorilla", NpcCFrame = CFrame.new(-1598, 36, 153)}
    elseif level >= 60 and level < 90 then
        return {QuestName = "BuggyQuest1", QuestNum = 1, MobName = "Pirate", NpcCFrame = CFrame.new(-1141, 4, 3832)}
    elseif level >= 90 and level < 120 then
        return {QuestName = "DesertQuest", QuestNum = 1, MobName = "Desert Bandit", NpcCFrame = CFrame.new(894, 6, 4386)}
    elseif level >= 120 and level < 150 then
        return {QuestName = "SnowQuest", QuestNum = 1, MobName = "Snow Bandit", NpcCFrame = CFrame.new(1385, 87, -1298)}
    elseif level >= 150 and level < 190 then
        return {QuestName = "MarineQuest2", QuestNum = 1, MobName = "Chief Petty Officer", NpcCFrame = CFrame.new(-5035, 20, 4324)}
    elseif level >= 190 and level < 275 then
        return {QuestName = "SkyQuest", QuestNum = 1, MobName = "Sky Bandit", NpcCFrame = CFrame.new(-4842, 717, -2623)}
    elseif level >= 275 and level < 375 then
        return {QuestName = "ColosseumQuest", QuestNum = 1, MobName = "Toga Warrior", NpcCFrame = CFrame.new(-1580, 7, -2982)}
    elseif level >= 375 and level < 450 then
        return {QuestName = "MagmaQuest", QuestNum = 1, MobName = "Military Soldier", NpcCFrame = CFrame.new(-5313, 12, 8515)}
    elseif level >= 450 and level < 525 then
        return {QuestName = "FishmanQuest", QuestNum = 1, MobName = "Fishman Warrior", NpcCFrame = CFrame.new(61122, 18, 1569)}
    elseif level >= 525 and level < 625 then
        return {QuestName = "SkyQuest2", QuestNum = 1, MobName = "God's Guard", NpcCFrame = CFrame.new(-7906, 5611, -2280)}
    elseif level >= 625 and level < 700 then
        return {QuestName = "FountainQuest", QuestNum = 1, MobName = "Corporal", NpcCFrame = CFrame.new(5259, 38, 4050)}
    
    -- Second Sea (700 - 1500)
    elseif level >= 700 and level < 775 then
        return {QuestName = "Area1Quest", QuestNum = 1, MobName = "Raider", NpcCFrame = CFrame.new(-425, 73, 1836)}
    elseif level >= 775 and level < 875 then
        return {QuestName = "Area2Quest", QuestNum = 1, MobName = "Mercenary", NpcCFrame = CFrame.new(638, 73, 918)}
    elseif level >= 875 and level < 1000 then
        return {QuestName = "SwanQuest", QuestNum = 1, MobName = "Swan Pirate", NpcCFrame = CFrame.new(878, 122, 1235)}
    elseif level >= 1000 and level < 1125 then
        return {QuestName = "ZombieQuest", QuestNum = 1, MobName = "Zombie", NpcCFrame = CFrame.new(-5492, 48, -795)}
    elseif level >= 1125 and level < 1250 then
        return {QuestName = "SnowMountainQuest", QuestNum = 1, MobName = "Snow Trooper", NpcCFrame = CFrame.new(609, 401, -5372)}
    elseif level >= 1250 and level < 1350 then
        return {QuestName = "IceSideQuest", QuestNum = 1, MobName = "Lab Subordinate", NpcCFrame = CFrame.new(-6064, 16, -4927)}
    elseif level >= 1350 and level < 1425 then
        return {QuestName = "FireSideQuest", QuestNum = 1, MobName = "Magma Ninja", NpcCFrame = CFrame.new(-5430, 16, -5296)}
    elseif level >= 1425 and level < 1500 then
        return {QuestName = "WaterFighterQuest", QuestNum = 1, MobName = "Water Fighter", NpcCFrame = CFrame.new(2888, 14, -5283)}
        
    -- Third Sea (1500 - 2800)
    elseif level >= 1500 and level < 1575 then
        return {QuestName = "PiratePortQuest", QuestNum = 1, MobName = "Pirate Millionaire", NpcCFrame = CFrame.new(-290, 44, 5580)}
    elseif level >= 1575 and level < 1700 then
        return {QuestName = "AmazonQuest", QuestNum = 1, MobName = "Pistol Billionaire", NpcCFrame = CFrame.new(5833, 52, -1100)}
    elseif level >= 1700 and level < 1825 then
        return {QuestName = "MarineTreeQuest", QuestNum = 1, MobName = "Marine Commodore", NpcCFrame = CFrame.new(2182, 29, -6737)}
    elseif level >= 1825 and level < 1975 then
        return {QuestName = "DeepForestIslandQuest", QuestNum = 1, MobName = "Forest Pirate", NpcCFrame = CFrame.new(-13233, 332, -7626)}
    elseif level >= 1975 and level < 2075 then
        return {QuestName = "HauntedQuest1", QuestNum = 1, MobName = "Reborn Skeleton", NpcCFrame = CFrame.new(-9482, 142, 5565)}
    elseif level >= 2075 and level < 2200 then
        return {QuestName = "PeanutQuest", QuestNum = 1, MobName = "Peanut Scout", NpcCFrame = CFrame.new(-2124, 38, -10237)}
    elseif level >= 2200 and level < 2450 then
        return {QuestName = "IceCreamQuest1", QuestNum = 1, MobName = "Ice Cream Chef", NpcCFrame = CFrame.new(-822, 66, -10965)}
    elseif level >= 2450 and level < 2600 then
        return {QuestName = "ChocolatierQuest", QuestNum = 1, MobName = "Chocolate Bar Battler", NpcCFrame = CFrame.new(151, 24, -12774)}
    elseif level >= 2600 and level <= 2800 then
        return {QuestName = "CandyQuest1", QuestNum = 1, MobName = "Candy Rebel", NpcCFrame = CFrame.new(-1148, 14, -14446)}
    end
end

-- Main Loop
task.spawn(function()
    while task.wait(0.1) do
        if _G.AutoFarm then
            pcall(function()
                local questInfo = getQuestData()
                if not questInfo then return end

                local mainGui = LocalPlayer.PlayerGui:FindFirstChild("Main")
                local questGui = mainGui and mainGui:FindFirstChild("Quest")
                
                -- Check if player has an active quest
                if not questGui or not questGui.Visible then
                    bypassTween(questInfo.NpcCFrame)
                    if (LocalPlayer.Character.HumanoidRootPart.Position - questInfo.NpcCFrame.Position).Magnitude <= 15 then
                        local commF = ReplicatedStorage:FindFirstChild("Remotes") and ReplicatedStorage.Remotes:FindFirstChild("CommF_")
                        if commF then
                            commF:InvokeServer("StartQuest", questInfo.QuestName, questInfo.QuestNum)
                        end
                    end
                else
                    -- Search and attack target mob
                    local enemies = workspace:FindFirstChild("Enemies")
                    local targetMob = nil
                    
                    if enemies then
                        for _, mob in pairs(enemies:GetChildren()) do
                            if mob.Name == questInfo.MobName and mob:FindFirstChild("Humanoid") and mob.Humanoid.Health > 0 and mob:FindFirstChild("HumanoidRootPart") then
                                targetMob = mob
                                break
                            end
                        end
                    end

                    if targetMob then
                        -- Hover above mob (8 units)
                        local mobPos = targetMob.HumanoidRootPart.CFrame * CFrame.new(0, 8, 0)
                        bypassTween(mobPos)
                        
                        -- Equip weapon and attack
                        equipWeapon()
                        VirtualUser:CaptureController()
                        VirtualUser:ClickButton1(Vector2.new(500, 500))
                    end
                end
            end)
        end
    end
end)
