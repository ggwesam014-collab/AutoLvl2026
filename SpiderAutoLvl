-- =================================================================
-- Redz Hub - Auto Farm Level (Blox Fruits Update 30 / Level 2800)
-- =================================================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local CommF = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("CommF_")

_G.AutoFarm = false

-- =================================================================
-- 1. جدول مهام التحديث 30 (Up to Level 2800)
-- =================================================================
local QuestsData = {
    ["ThirdSea"] = {
        {MinLevel = 2200, MaxLevel = 2299, QuestName = "PeanutQuest", QuestNPC = CFrame.new(-2100, 38, -10190), EnemyName = "Peanut Scout", EnemyCFrame = CFrame.new(-2200, 38, -10190)},
        {MinLevel = 2300, MaxLevel = 2399, QuestName = "IceCreamQuest", QuestNPC = CFrame.new(-820, 65, -10960), EnemyName = "Ice Cream Chef", EnemyCFrame = CFrame.new(-950, 65, -10960)},
        {MinLevel = 2400, MaxLevel = 2474, QuestName = "TikiQuest1", QuestNPC = CFrame.new(-16234, 9, 483), EnemyName = "Isle Outlaw", EnemyCFrame = CFrame.new(-16000, 20, 600)},
        {MinLevel = 2475, MaxLevel = 2599, QuestName = "TikiQuest2", QuestNPC = CFrame.new(-16234, 9, 483), EnemyName = "Island Boy", EnemyCFrame = CFrame.new(-16400, 20, 800)},
        -- مهام التحديث 30 الجديد (Level 2600 - 2800)
        {MinLevel = 2600, MaxLevel = 2699, QuestName = "MagnetIsleQuest1", QuestNPC = CFrame.new(-17100, 35, 1200), EnemyName = "Scrap Collector", EnemyCFrame = CFrame.new(-17250, 40, 1350)},
        {MinLevel = 2700, MaxLevel = 2800, QuestName = "MagnetIsleQuest2", QuestNPC = CFrame.new(-17100, 35, 1200), EnemyName = "Magnetic Enforcer", EnemyCFrame = CFrame.new(-17400, 40, 1500)}
    }
}

-- =================================================================
-- 2. Smooth Custom Tween (Anti-Bypass System)
-- =================================================================
local function CustomTween(targetCFrame, speed)
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return end
    
    local hrp = character.HumanoidRootPart
    speed = speed or 350

    local bodyVelocity = Instance.new("BodyVelocity")
    bodyVelocity.Velocity = Vector3.new(0, 0, 0)
    bodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    bodyVelocity.Parent = hrp

    local noclip = RunService.Stepped:Connect(function()
        for _, part in pairs(character:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide = false end
        end
    end)

    local distance = (hrp.Position - targetCFrame.Position).Magnitude
    local travelTime = distance / speed
    local startTime = tick()

    while (tick() - startTime) < travelTime and _G.AutoFarm do
        if not character or not character:FindFirstChild("HumanoidRootPart") then break end
        local alpha = (tick() - startTime) / travelTime
        hrp.CFrame = hrp.CFrame:Lerp(targetCFrame, alpha)
        RunService.Heartbeat:Wait()
    end

    if hrp then hrp.CFrame = targetCFrame end
    bodyVelocity:Destroy()
    noclip:Disconnect()
end

-- =================================================================
-- 3. Fast Attack System (موافق للتحديث 30)
-- =================================================================
local function FastAttack()
    pcall(function()
        local weapon = LocalPlayer.Character:FindFirstChildOfClass("Tool")
        if weapon then
            ReplicatedStorage.Modules.Net:FindFirstChild("RE/RegisterAttack"):FireServer(0)
            ReplicatedStorage.Modules.Net:FindFirstChild("RE/RegisterHit"):FireServer(weapon)
        end
    end)
end

-- =================================================================
-- 4. الحلقة الرئيسية لـ Auto Farm
-- =================================================================
local function GetMyQuestData()
    local level = LocalPlayer.Data.Level.Value
    for _, quest in pairs(QuestsData["ThirdSea"]) do
        if level >= quest.MinLevel and level <= quest.MaxLevel then
            return quest
        end
    end
    return QuestsData["ThirdSea"][#QuestsData["ThirdSea"]]
end

task.spawn(function()
    while true do
        task.wait(0.05)
        if _G.AutoFarm then
            local questData = GetMyQuestData()
            if questData then
                local myGui = LocalPlayer.PlayerGui:FindFirstChild("Main")
                local hasQuest = myGui and myGui:FindFirstChild("Quest") and myGui.Quest.Visible
                
                if not hasQuest then
                    CustomTween(questData.QuestNPC * CFrame.new(0, 0, 3), 350)
                    task.wait(0.3)
                    CommF:InvokeServer("StartQuest", questData.QuestName, 1)
                else
                    local targetPos = questData.EnemyCFrame * CFrame.new(0, 9, 0)
                    if (LocalPlayer.Character.HumanoidRootPart.Position - targetPos.Position).Magnitude > 15 then
                        CustomTween(targetPos, 350)
                    else
                        LocalPlayer.Character.HumanoidRootPart.CFrame = targetPos
                        FastAttack()
                    end
                end
            end
        end
    end
end)

-- =================================================================
-- 5. UI Redz Hub
-- =================================================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "RedzHubUpdate30"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = gethui and gethui() or CoreGui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 290, 0, 160)
MainFrame.Position = UDim2.new(0.5, -145, 0.4, -80)
MainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 35)
Title.Text = "Redz Hub - Update 30 [Lvl 2800]"
Title.TextColor3 = Color3.fromRGB(255, 50, 50)
Title.TextSize = 14
Title.Font = Enum.Font.SourceSansBold
Title.Parent = MainFrame

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(0.85, 0, 0, 45)
ToggleBtn.Position = UDim2.new(0.075, 0, 0.45, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 30, 30)
ToggleBtn.Text = "Auto Farm: OFF"
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.TextSize = 16
ToggleBtn.Font = Enum.Font.SourceSansBold
ToggleBtn.Parent = MainFrame

local BtnCorner = Instance.new("UICorner")
BtnCorner.CornerRadius = UDim.new(0, 8)
BtnCorner.Parent = ToggleBtn

ToggleBtn.MouseButton1Click:Connect(function()
    _G.AutoFarm = not _G.AutoFarm
    ToggleBtn.Text = _G.AutoFarm and "Auto Farm: ON" or "Auto Farm: OFF"
    ToggleBtn.BackgroundColor3 = _G.AutoFarm and Color3.fromRGB(30, 180, 80) or Color3.fromRGB(180, 30, 30)
end)

local ToggleUI = Instance.new("TextButton")
ToggleUI.Size = UDim2.new(0, 45, 0, 45)
ToggleUI.Position = UDim2.new(0, 15, 0.5, -22)
ToggleUI.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
ToggleUI.Text = "REDZ"
ToggleUI.TextColor3 = Color3.fromRGB(255, 50, 50)
ToggleUI.TextSize = 12
ToggleUI.Font = Enum.Font.SourceSansBold
ToggleUI.Active = true
ToggleUI.Draggable = true
ToggleUI.Parent = ScreenGui

local UICorner2 = Instance.new("UICorner")
UICorner2.CornerRadius = UDim.new(1, 0)
UICorner2.Parent = ToggleUI

ToggleUI.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)
