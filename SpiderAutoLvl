-- =================================================================
-- Redz Hub Style - Auto Farm Level (Dynamic & Smooth Speed 300)
-- =================================================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local CommF = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("CommF_")

_G.AutoFarm = false

-- =================================================================
-- 1. جدول أسم المهام والوحوش فقط (دون الحاجة لإحداثيات ثابتة)
-- =================================================================
local QuestsData = {
    ["FirstSea"] = {
        {MinLevel = 1, MaxLevel = 14, QuestName = "BanditQuest1", EnemyName = "Bandit", NPCName = "Bandit Quest Giver"},
        {MinLevel = 15, MaxLevel = 29, QuestName = "JungleQuest", EnemyName = "Monkey", NPCName = "Jungle Quest Giver"},
        {MinLevel = 30, MaxLevel = 59, QuestName = "PirateQuest", EnemyName = "Pirate", NPCName = "Pirate Quest Giver"},
        {MinLevel = 60, MaxLevel = 89, QuestName = "SnowQuest", EnemyName = "Snow Bandit", NPCName = "Snow Quest Giver"},
        {MinLevel = 90, MaxLevel = 119, QuestName = "MarineQuest2", EnemyName = "Chief Petty Officer", NPCName = "Marine Quest Giver"},
        {MinLevel = 120, MaxLevel = 149, QuestName = "SkyQuest", EnemyName = "Sky Bandit", NPCName = "Sky Quest Giver"},
        {MinLevel = 150, MaxLevel = 189, QuestName = "PrisonerQuest", EnemyName = "Prisoner", NPCName = "Prisoner Quest Giver"},
        {MinLevel = 190, MaxLevel = 249, QuestName = "ColosseumQuest", EnemyName = "Toga Warrior", NPCName = "Colosseum Quest Giver"},
        {MinLevel = 250, MaxLevel = 299, QuestName = "MagmaQuest", EnemyName = "Military Soldier", NPCName = "Magma Quest Giver"},
        {MinLevel = 300, MaxLevel = 374, QuestName = "FishmanQuest", EnemyName = "Fishman Warrior", NPCName = "Fishman Quest Giver"},
        {MinLevel = 375, MaxLevel = 449, QuestName = "SkyExp1Quest", EnemyName = "God's Guard", NPCName = "Sky Exp1 Quest Giver"},
        {MinLevel = 450, MaxLevel = 699, QuestName = "FountainQuest", EnemyName = "Galley Pirate", NPCName = "Fountain Quest Giver"}
    },
    ["SecondSea"] = {
        {MinLevel = 700, MaxLevel = 774, QuestName = "Area1Quest", EnemyName = "Raider", NPCName = "Area1 Quest Giver"},
        {MinLevel = 775, MaxLevel = 874, QuestName = "Area2Quest", EnemyName = "Mercenary", NPCName = "Area2 Quest Giver"},
        {MinLevel = 875, MaxLevel = 949, QuestName = "SwanQuest", EnemyName = "Swan Pirate", NPCName = "Swan Quest Giver"},
        {MinLevel = 950, MaxLevel = 999, QuestName = "ZombieQuest", EnemyName = "Zombie", NPCName = "Zombie Quest Giver"},
        {MinLevel = 1000, MaxLevel = 1099, QuestName = "SnowMountainQuest", EnemyName = "Snow Trooper", NPCName = "Snow Mountain Quest Giver"},
        {MinLevel = 1100, MaxLevel = 1174, QuestName = "IceSideQuest", EnemyName = "Lab Subordinate", NPCName = "Ice Side Quest Giver"},
        {MinLevel = 1175, MaxLevel = 1249, QuestName = "FireSideQuest", EnemyName = "Horned Warrior", NPCName = "Fire Side Quest Giver"},
        {MinLevel = 1250, MaxLevel = 1349, QuestName = "ShipQuest1", EnemyName = "Ship Deckhand", NPCName = "Ship Quest Giver"},
        {MinLevel = 1350, MaxLevel = 1424, QuestName = "FrostQuest", EnemyName = "Arctic Warrior", NPCName = "Frost Quest Giver"},
        {MinLevel = 1425, MaxLevel = 1499, QuestName = "ForgottenQuest", EnemyName = "Sea Soldier", NPCName = "Forgotten Quest Giver"}
    },
    ["ThirdSea"] = {
        {MinLevel = 1500, MaxLevel = 1574, QuestName = "PiratePortQuest", EnemyName = "Pirate Millionaire", NPCName = "Pirate Port Quest Giver"},
        {MinLevel = 1575, MaxLevel = 1699, QuestName = "AmazonQuest", EnemyName = "Pistol Billionaire", NPCName = "Amazon Quest Giver"},
        {MinLevel = 1700, MaxLevel = 1799, QuestName = "MarineTreeQuest", EnemyName = "Marine Commodore", NPCName = "Marine Tree Quest Giver"},
        {MinLevel = 1800, MaxLevel = 1899, QuestName = "FloatingTurtleQuest", EnemyName = "Fishman Raider", NPCName = "Floating Turtle Quest Giver"},
        {MinLevel = 1900, MaxLevel = 1999, QuestName = "CastleQuest", EnemyName = "Corporal", NPCName = "Castle Quest Giver"},
        {MinLevel = 2000, MaxLevel = 2074, QuestName = "HauntedQuest1", EnemyName = "Reborn Skeleton", NPCName = "Haunted Quest Giver"},
        {MinLevel = 2075, MaxLevel = 2199, QuestName = "HauntedQuest2", EnemyName = "Demonic Soul", NPCName = "Haunted Quest Giver"},
        {MinLevel = 2200, MaxLevel = 2299, QuestName = "PeanutQuest", EnemyName = "Peanut Scout", NPCName = "Peanut Quest Giver"},
        {MinLevel = 2300, MaxLevel = 2399, QuestName = "IceCreamQuest", EnemyName = "Ice Cream Chef", NPCName = "Ice Cream Quest Giver"},
        {MinLevel = 2400, MaxLevel = 2474, QuestName = "TikiQuest1", EnemyName = "Isle Outlaw", NPCName = "Tiki Quest Giver 1"},
        {MinLevel = 2475, MaxLevel = 2800, QuestName = "TikiQuest2", EnemyName = "Island Boy", NPCName = "Tiki Quest Giver 2"}
    }
}

-- =================================================================
-- 2. دالة كشف البحر والبيانات
-- =================================================================
local function GetCurrentSea()
    local placeId = game.PlaceId
    if placeId == 2753915549 then return "FirstSea"
    elseif placeId == 4442272183 then return "SecondSea"
    elseif placeId == 7449423635 then return "ThirdSea"
    else return "SecondSea" end
end

local function GetMyQuestData()
    local sea = GetCurrentSea()
    local level = LocalPlayer.Data.Level.Value
    for _, quest in pairs(QuestsData[sea]) do
        if level >= quest.MinLevel and level <= quest.MaxLevel then
            return quest
        end
    end
    return QuestsData[sea][#QuestsData[sea]]
end

-- =================================================================
-- 3. نظام البحث المباشر عن الوحوش والـ NPCs في الـ Workspace
-- =================================================================
local function GetEnemyModel(enemyName)
    for _, v in pairs(workspace.Enemies:GetChildren()) do
        if v.Name == enemyName and v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 then
            if v:FindFirstChild("HumanoidRootPart") then
                return v
            end
        end
    end
    return nil
end

local function GetNPCPosition(npcName)
    local npc = workspace.NPCs:FindFirstChild(npcName) or workspace:FindFirstChild(npcName)
    if npc and npc:FindFirstChild("HumanoidRootPart") then
        return npc.HumanoidRootPart.CFrame
    end
    return nil
end

-- =================================================================
-- 4. Smooth Redz-Style Tween (Speed 300) + Anti-Collision
-- =================================================================
local function RedzTween(targetCFrame)
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return end
    
    local hrp = character.HumanoidRootPart
    local speed = 300 -- نفس سرعة Redz Hub المضبوطة للسيرفرات

    local bodyVelocity = Instance.new("BodyVelocity")
    bodyVelocity.Velocity = Vector3.new(0, 0, 0)
    bodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    bodyVelocity.Parent = hrp

    -- No-Clip كامل طوال الحركة
    local noclipConnection = RunService.Stepped:Connect(function()
        for _, part in pairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
    end)

    local distance = (hrp.Position - targetCFrame.Position).Magnitude
    local travelTime = distance / speed
    local startTime = tick()

    while (tick() - startTime) < travelTime and _G.AutoFarm do
        if not character or not character:FindFirstChild("HumanoidRootPart") then break end
        local alpha = (tick() - startTime) / travelTime
        hrp.CFrame = hrp.CFrame:Lerp(targetCFrame, alpha)
        RunService.Heartbeat:Wait()
    end

    if hrp then hrp.CFrame = targetCFrame end
    bodyVelocity:Destroy()
    noclipConnection:Disconnect()
end

-- =================================================================
-- 5. Fast Attack
-- =================================================================
local function FastAttack()
    pcall(function()
        local weapon = LocalPlayer.Character:FindFirstChildOfClass("Tool")
        if weapon then
            ReplicatedStorage.Modules.Net:FindFirstChild("RE/RegisterAttack"):FireServer(0)
            ReplicatedStorage.Modules.Net:FindFirstChild("RE/RegisterHit"):FireServer(weapon)
        end
    end)
end

-- =================================================================
-- 6. الحلقة الرئيسية لـ Auto Farm
-- =================================================================
task.spawn(function()
    while true do
        task.wait(0.05)
        if _G.AutoFarm then
            local questData = GetMyQuestData()
            if questData then
                local myGui = LocalPlayer.PlayerGui:FindFirstChild("Main")
                local hasQuest = myGui and myGui:FindFirstChild("Quest") and myGui.Quest.Visible

                if not hasQuest then
                    -- التوجه المباشر للـ NPC المأخوذ من الخريطة
                    local npcPos = GetNPCPosition(questData.NPCName)
                    if npcPos then
                        RedzTween(npcPos * CFrame.new(0, 2, 3))
                        task.wait(0.2)
                        CommF:InvokeServer("StartQuest", questData.QuestName, 1)
                    end
                else
                    -- البحث عن الوحش الحي والتنقل فوقه
                    local enemy = GetEnemyModel(questData.EnemyName)
                    if enemy then
                        local targetCFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0, 9, 0)
                        if (LocalPlayer.Character.HumanoidRootPart.Position - targetCFrame.Position).Magnitude > 15 then
                            RedzTween(targetCFrame)
                        else
                            LocalPlayer.Character.HumanoidRootPart.CFrame = targetCFrame
                            FastAttack()
                        end
                    end
                end
            end
        end
    end
end)

-- =================================================================
-- 7. UI Redz Hub
-- =================================================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "RedzHubExactSpeed"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = gethui and gethui() or CoreGui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 280, 0, 150)
MainFrame.Position = UDim2.new(0.5, -140, 0.4, -75)
MainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 35)
Title.Text = "Redz Hub - Dynamic & Speed 300"
Title.TextColor3 = Color3.fromRGB(255, 50, 50)
Title.TextSize = 13
Title.Font = Enum.Font.SourceSansBold
Title.Parent = MainFrame

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(0.85, 0, 0, 45)
ToggleBtn.Position = UDim2.new(0.075, 0, 0.45, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 30, 30)
ToggleBtn.Text = "Auto Farm: OFF"
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.TextSize = 16
ToggleBtn.Font = Enum.Font.SourceSansBold
ToggleBtn.Parent = MainFrame

local BtnCorner = Instance.new("UICorner")
BtnCorner.CornerRadius = UDim.new(0, 8)
BtnCorner.Parent = ToggleBtn

ToggleBtn.MouseButton1Click:Connect(function()
    _G.AutoFarm = not _G.AutoFarm
    ToggleBtn.Text = _G.AutoFarm and "Auto Farm: ON" or "Auto Farm: OFF"
    ToggleBtn.BackgroundColor3 = _G.AutoFarm and Color3.fromRGB(30, 180, 80) or Color3.fromRGB(180, 30, 30)
end)

local ToggleUI = Instance.new("TextButton")
ToggleUI.Size = UDim2.new(0, 45, 0, 45)
ToggleUI.Position = UDim2.new(0, 15, 0.5, -22)
ToggleUI.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
ToggleUI.Text = "REDZ"
ToggleUI.TextColor3 = Color3.fromRGB(255, 50, 50)
ToggleUI.TextSize = 12
ToggleUI.Font = Enum.Font.SourceSansBold
ToggleUI.Active = true
ToggleUI.Draggable = true
ToggleUI.Parent = ScreenGui

local UICorner2 = Instance.new("UICorner")
UICorner2.CornerRadius = UDim.new(1, 0)
UICorner2.Parent = ToggleUI

ToggleUI.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)
